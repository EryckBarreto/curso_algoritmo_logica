Algoritmo  "circulo"


Var

   area, pii, raio : real


Inicio

   escreva("Digite o valor do raio do círculo: ")
   leia(raio)
   
   pii <- pi
   area <- pii * (Exp(raio, 2))
   
   escreva("AREA = ", area:8:3)



Fimalgoritmo